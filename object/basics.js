//let myBook = {
    //title: '1984',
    //author: 'George Orwell',
    //pageCount: 362
//}

//console.log(`${myBook.title} by ${myBook.author}`)

//myBook.title = `animal Farm`

//console.log(`${myBook.title} by ${myBook.author}`)


let person = {
    name: `Bergerac`,
    age: `20`,
    location: `Kigali`
}

console.log(`${person.name} is ${person.age} and lives in
${person.location}`)

person.age = `21`

console.log(`${person.name} is ${person.age} and lives in
${person.location}`)